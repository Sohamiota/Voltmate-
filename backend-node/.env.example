# Example environment variables for local development
# WARNING: Do NOT commit real secrets to version control.
DATABASE_URL=postgres://username:password@localhost:5432/dbname
JWT_SECRET=REPLACE_WITH_A_STRONG_RANDOM_SECRET
ALLOWED_ORIGIN=http://localhost:3000
SENDGRID_API_KEY=
SENDGRID_FROM=no-reply@example.com
NODE_ENV=development

# Usage:
# - Copy this file to `.env` in `backend-node/` for local development.
# - Replace the placeholder values with real credentials.
# - Do NOT commit the `.env` file containing secrets.
