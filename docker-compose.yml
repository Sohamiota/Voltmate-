version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend-node
      dockerfile: Dockerfile
    environment:
      - DATABASE_URL=postgres://postgres:password@db:5432/postgres?sslmode=disable
      - JWT_SECRET=6f9d3a8b2e1f4c7a9d5b6e2a3c4f7d1b2a9c5e7f8d3b6a1c4e9f2b7a6c3d8e1
      - PORT=8080
      - SENDGRID_API_KEY
      - SENDGRID_FROM
    depends_on:
      - db
    ports:
      - "8081:8080"

  frontend:
    build:
      context: ./Voltmate-
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    depends_on:
      - backend

volumes:
  db-data:
