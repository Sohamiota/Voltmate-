# Frontend production env (example) - DO NOT commit secrets
# Set this when building so the app loads data from your backend. No trailing /api/v1.
# Example if backend is on Render:
NEXT_PUBLIC_API_URL=https://voltmate.onrender.com
